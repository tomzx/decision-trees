import computer.browse
import generic.go_to

def call(person):
	# Precondition
	generic.go_to.call(person, 'computer')
	# Actions
	computer.browse.call(person, 'gmail.com')
	computer.browse.call(person, 'github.com')
	print('check omnifocus')
	print('check hexchat')
	print('check mail')
