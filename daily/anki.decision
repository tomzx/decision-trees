import generic.go_to

def call(person):
	# Precondition
	generic.go_to.call(person, 'computer')
	# Actions
	print('open anki')
	print('study deck Tom')
	studied_cards = []
	for card in studied_cards:
		print('recall card')
	print('record day statistics')